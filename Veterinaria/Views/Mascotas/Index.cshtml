@model IEnumerable<Veterinaria.Models.Mascotas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Dueño.nomDueño)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.nomMascota)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.peso)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.raza)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaNacimiento)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Dueño.nomDueño)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.nomMascota)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.peso)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.raza)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaNacimiento)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.idMascota }) |
            @Html.ActionLink("Details", "Details", new { id=item.idMascota }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.idMascota })
        </td>
    </tr>
}

</table>
